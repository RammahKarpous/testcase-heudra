{%- assign heading_styling = 'text-2xl font-bold text-blue-medium font-light mb-4' -%}

<section class="absolute left-0 z-10 invisible w-screen pt-6 pb-10 bg-body top-full group-hover/main-nav:visible">
  <div class="max-w-screen-xl inset-y-0 grid grid-cols-1 gap-8 px-4 mx-auto lg:grid-cols-[1fr_300px]">
    <ul>
      <h3 class="{{ heading_styling }}">Onze Categoriën</h3>

      <div class="grid grid-cols-3 gap-x-8 gap-y-2">
        {%- for collection in collections -%}
          <li>
            {%- render 'component-navigation-bordered-link',
              link_text: collection.title,
              link_url: collection.url,
              has_graphic: true,
              styles: 'bg-white'
            -%}
          </li>
        {%- endfor -%}
      </div>
    </ul>

    <ul>
      <h3 class="{{ heading_styling }}">Meer aanhangwagens</h3>

      <div class="flex flex-col gap-2">
        
      </div>
    </ul>
  </div>
</section>
